require 'spec_helper'

describe Contractinator do
  it 'has a version number' do
    expect(Contractinator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
